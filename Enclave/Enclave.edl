enclave {
    from "Sealing/Sealing.edl" import *;

    trusted {
        /* define ECALLs here. */
        //public int generate_random_number(void);
        public char* get_password([in, string] char* encrypted_string);
        public char* add_password([in, string] char* password);
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print([in, string]const char* str);
    };
};
