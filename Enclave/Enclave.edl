enclave {
    from "Sealing/Sealing.edl" import *;

    trusted {
        /* define ECALLs here. */
        //public int generate_random_number(void);
        public int add_password([in, string] char* password);
        public int get_password([out, size=buffer_size] char* encrypted_string, unsigned buffer_size);
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print([in, string]const char* str);
    };
};
